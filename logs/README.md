# logs — Runtime Structured Logs (gitignored)

Destination for `structlog` JSON output (the Logging Layer,
`context/01_blueprint.md` §4) — the explainability audit trail of every agent
decision. Gitignored entirely (see `.gitignore`); only this README and
`.gitkeep` are tracked so the directory exists on fresh clones.

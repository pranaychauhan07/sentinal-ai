# reports_out — Generated PDF Output (gitignored)

Runtime destination for `core/reporting/pdf_builder.py` output. Contents are
gitignored (see `.gitignore`); this file plus `.gitkeep` are the only tracked
items so the directory exists on fresh clones.
